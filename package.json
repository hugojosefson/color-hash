{
  "name": "@hugojosefson/color-hash",
  "version": "1.0.3",
  "description": "Generate color based on the given string (using HSL color space and BKDRHash).",
  "main": "src/index.js",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "prepare": "run-s build",
    "build": "browserify src --standalone ColorHash --outfile dist/color-hash.js",
    "test": "istanbul cover _mocha"
  },
  "repository": "hugojosefson/color-hash",
  "author": "Zeno Zeng",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hugojosefson/color-hash/issues"
  },
  "devDependencies": {
    "browserify": "^14.4.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.5.3",
    "npm-run-all": "^4.1.1",
    "rewire": "^2.1.5",
    "string-hash": "^1.1.3"
  },
  "dependencies": {},
  "engines": {
    "node": ">=6"
  }
}
